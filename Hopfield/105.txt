#include <iostream>
using namespace std;

const size_t  rows = 600;
const size_t col = 4;
int n;
int matrix[6][4] = { {32, 41, 42, 43}, {25, 31, 32, 0}, {24, 33, 34, 0}, {13, 24, 25, 0},
						{11, 21, 22, 23}, {1, 11, 12, 13} };
int monk[5];

/*Работает, но надо исправить ввод*/
auto monk_history() {
	int monk_num;
	cout << "monk_num" << endl;
	cin >> monk_num;
	if (monk_num < 600 && monk_num > 0) {
		for (int i = 0; i < rows; ++i) {
			for (int j = 0; j < col; ++j) {
				if (monk_num == matrix[i][j]) {
					cout << "teacher is " << matrix[i][0];
				}
			}
		}
	}
	else {
		cout << "Wrong input" << endl;
	}
}

void Search() {
	if (n == 1) {
		monk_history();
	}
	else if (n == 2) {
		// найти общего учителя
	}
}

/*Работает, но нужно чтобы выводило число.*/
void Input(int n) {
	cout << "Начните вводить числа" << endl;
	for (int i = 0; i < n; ++ i) {
		cin >> monk[i];
		++i;
		cout << i << endl;
	}

}

/*Работает*/
auto MakeMartix() { // Создаем матрицу
	for (int i = 0; i < rows; ++i) {
		for (int j = 0; j < col; ++j) {
			cin >> matrix[i][j];
		}
	}
	return matrix;
}



int main() {
	setlocale(LC_ALL, "Rus");
	cin >> n;
	Input(n);
	monk_history();
	system("Pause");
	return 0;
}
